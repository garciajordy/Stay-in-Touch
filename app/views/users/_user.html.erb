<li>
    Name: <%= user.name %>
    <% if user.id != current_user.id && !@friends  %>
    <%= friend_or_unfriend_btn(user) %>
    <% end %>
    <span class="profile-link">
        <%= link_to 'See Profile',  user_path(user), class: 'profile-link' %>
    </span>


</li>